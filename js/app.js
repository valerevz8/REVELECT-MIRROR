document.addEventListener('DOMContentLoaded', () => {
  const root = document.querySelector('main');

  root.innerHTML = `
    <section class="home-shell">
      <p class="eyebrow">REVELECT</p>
      <h1>One reflection. One insight.</h1>
      <p>A guided space to pause, notice, and see yourself differently.</p>
      <a class="primary-link" href="reflection.html">Start Reflection</a>
    </section>
  `;
});
